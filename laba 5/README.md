# Лабораторная работа № 5
___
При решении был создан вспомогательный класс **Metod** для проверок и сокращения кода в **Main**.  
```java
//проверка на число
    public static boolean isInt(String s) {
        if (s != null && !s.isEmpty()) { //проверяем на пустую строчку или null
            try {
                int number = Integer.parseInt(s); //пробуем преобразовать строку в число
                return true;
            } catch (NumberFormatException e) { //ошибка, если не получилось
                System.out.println("Вы ввели не число!(или были введены помимо числа пробелы)");
                return false;
            }
        } else {
            System.out.println("Строка пуста (или null)!");
            return false;
        }
    }
```
```java
//цикл проверок, пока число не подойдёт
    public static int chekInt() {
        Scanner scanner = new Scanner(System.in);
        String n = scanner.nextLine();
         while (!Metod.isInt(n)) {
             System.out.print("Введите новое значение: ");
             n = scanner.nextLine();
         }
         int num = Integer.parseInt(n);
         return num;
    }
```
```java
    public static boolean isVoid(String s) {
        if (s==null || s.isEmpty()) { //строка null или пуста
            return false;
        } else {
            return true;
        }
    }
```
---
## Задание 1. Шаблоны
```
В класс Дробь, добавить интерфейс на два метода: получение вещественного значения, установка
числителя и установка знаменателя.
Сгенерировать такую версию дроби, которая будет кэшировать вычисление вещественного
значения.
Если раннее в вашем варианте не было Дроби, то создайте сущность Дробь со следующими
особенностями:
• Имеет числитель: целое число
• Имеет знаменатель: целое число
• Дробь может быть создана с указанием числителя и знаменателя
• Может вернуть строковое представление вида “числитель/знаменатель”
• Необходимо корректно обрабатывать отрицательные значения. Учтите, что знаменатель не может
быть отрицательным.
• Переопределите метод сравнения объектов по состоянию таким образом, чтобы две дроби
считались одинаковыми тогда, когда у них одинаковые значения числителя и знаменателя.
```
Для решения задания были созданы:
- Интерфейс **FracInter**.
- Класс **Fraction**, который использует интерфейс **FracInter**.
- Класс **CachedFraction**, который наследуется от **Fraction**.
---
### Поговорим о каждом отдельно.  

### interface FracInter :  
В интерфейсе описано 3 метода.  
```
double getValue(); //результат вычисления числа
void setNumer(int numer); //внесение знаменателя
void setDenom(int denom); //внесение числителя
```

### Fraction implements FracInter :  

- **private int numer** - поле, отвечающее за числитель.  
- **private int denom** - поле, отвечает за знаменатель.  
- Геттеры **getNumer()**,**getDenom()** - методы для получения значений числителя и знаменателя.  
- Сеттер **setNumer(int numer)** - переопределённый метод для внесения числителя.  
- Сеттер **setDenom(int denom)** - переопределённый метод для внесения знаменателя. В нём есть проверка, чтобы внесённый параметр не был 0.
```java
if (denom == 0) {
    throw new IllegalStateException("Знаменатель не может равняться 0"); //ошибка, если значение равно 0
}

if (denom < 0) { //если было отрицательно, то меняем на на положительное. Так как знаменятель не может быть отрицательным
    this.numer = -this.numer; //переносим этот "минус" в числитель
    denom = -denom; // меняем знаменатель на положительный 
}
this.denom = denom; //вводим значение в поле
```
- Конструктор **Fraction(int numer, int denom)** - Для создания дроби с 2 параметрами. Где также есть проверка, 
что знаменатель не 0 и правильно обрабатывались отрицательные знаменатели.
- boolean **equals(Object obj)** - переопределённый метод для сравнения дробей
```java
if (this == obj) {
    return true; //ссылочное равенство
}
if (!(obj instanceof Fraction)) { //Проверка: объект obj аявляется экземпляром класса Fraction или его наследника?
    return false;
}
Fraction other = (Fraction) obj; //если до этого не было ошибки,то создаём сушьность дробь с сылкой на проверяемый объект
if (other.numer==numer && other.denom==denom) { //если числитель и знаменатель дроби равен знаменятелю и числителю другой дроби
    return true;
} else {
    return false;
}
```
- String **toString()** - переопределённый метод для строкового представления дроби.  
- int **hashCode()** - переопределённый метод для вычисления общего хэш-кода для числителя и знаменателя.  
```java
return Objects.hash(numer, denom); // hash = 31*numer + denom
```
### CachedFrac  extends Fraction:
- private **Double cachedV** - поле, хранящее последнее вычисленное вещественное значение дроби.При первом создании дроби равно null  
- **CachedFrac(int numer, int denom)** - конструктор для создания дроби. Использует 
конструктор родительского класса Fraction.
- **setNumer(int numer)** - переопределённый сеттер для внесения числителя в дробь. Использует сеттер родительского класса.  
- **setDenom(int denom)** - переопределённый сеттер для внесения знаменателя в дробь. Также используем метод из родительского класса.  
- **double getValue()** - переопределённый метод для получения вещественного числа.  
```java
if (cachedV == null) { //если до этого вещественное значение не было вычислено
    System.out.println("\nВычисляю");
    cachedV = super.getValue(); //вычисляем через родительский метод
}
return cachedV; // возвращаем значение
```
### Работа программы 
```
Сейчас вам предстоит создать 2 дроби

Создадим 1 дробь
Числитель: -4
Знаменатель: -10
Дробь: 4/10
Создадим 2 дробь
Числитель: 3
Знаменатель: 10
Дробь: 3/10
Одинаковы ли дроби: false

Вычисляю
Вещественное значение 1 дроби: 0.4
Вещественное значение 1 дроби: 0.4
Вычисляю

Вещественное значение 2 дроби: 0.3
Новый числитель 1 дроби: 3
1 дробь: 3/10

Вычисляю
Вещественное значение 1 дроби: 0.3
```
## Задание 2. Структурные шаблоны
```
Количество мяуканий.  
Необходимо воспользоваться классом Кот и методом принимающим всех мяукающих из задачи 2.5.4.
Необходимо таким образом передать кота в указанный метод, что бы после окончания его работы
узнать сколько раз мяукал кот за время его работы. На рисунке показан пример работы. Перед вызовом
метода создаем кота, отправляем ссылку на кота в метод, после окончания его работы выводим
количество мяуканий на экран. Кота изменять нельзя.
```
```
Meowable m = ....//создаём кота
Funs.meowsCare(m);
System.out.println(...) //вывод: кот мяукал 5 раз
```
```
Если раннее в вашем варианте не было Кота, то создайте
1. сущность Кот, которая описывается следующим образом:
• Имеет Имя (строка)
• Для создания необходимо указать имя кота.
• Может быть приведен к текстовой форме вида: “кот: Имя”
• Может помяукать, что приводит к выводу на экран следующего текста: “Имя: мяу!”,
вызвать мяуканье можно без параметров.
2. интерфейс Мяуканье: разработайте метод, который принимает набор объектов способных
мяукать и вызывает мяуканье у каждого объекта. Мяукающие объекты должны иметь метод со
следующей сигнатурой:
public void meow();
```
Для решения задания были созданы:
- Интерфейс **Meowable**.  
- Класс **Cat**, который использует интерфейс **Meowable**.  
- Класс **CountMeow**, который использует интерфейс **Meowable**.  
- Класс **Funs**.  
---
### Поговорим о каждом отдельно.

### interface Meowable:  
Определён один пустой метод **meow()**

### class Cat implements Meowable:  
- **final String name** - поле, отвечающее за имя.  
- **Cat(String name)** - конструктор для создания кота с именем.  
- **void meow()** - переопределённый метод для мяуканья.
- **String toString()** - переопределённый метод для вывода информации о коте.  

### CountMeow implements Meowable:  
- **final Meowable original** - поле с кем-то мяукающим.  
- **int count=0** - поле, счётчик мяуканий.  
- **CountMeow(Meowable original)** - конструктор для создания с 1 параметром.  
- **meow()** - переопределённый метод мяуканий. В нём ведется счёт о каждом "мяу".  
- **getCount()** - гетер для возврата количества мяуканий.  

###  class Funs:  
- **void meowsCare(Meowable ...arr)** - метод, принимающий набор объектов, способных мяукать и заставляет их мяукнуть.  
```java
for (Meowable m : arr) { //проходимся по набору
    m.meow(); //застывляем мяукнуть
}
```
### Работа программы 
```
Сейчас создадим котика.
Имя: Барсик
Барсик: мяу!
Барсик: мяу!
Барсик: мяу!
Барсик: мяу!
Кот мяукал 4 раз
```
## Задание 3. Список
```
Составить программу, которая удаляет из списка L все элементы E, если такие есть
```
Для решения использовался метод из класса **Metod**:  
```java
public static <T> void removeE(List<T> list, T element) { //используем дженерики для удобства
    list.removeIf(x -> x.equals(element)); //метод интерфейса Collection. Удаляет элемент из списка, если он
                                            //удовлетваряет лямба выражению, которое возвращает boolean
}
```
### Работа программы 
```
Введите количество элементов списка: 4
Введите элементы списка
Элемент: a
Элемент: a
Элемент: d
Элемент: x
Введите элемент E, который нужно удалить: x
Список после удаления: [a, a, d]
```